Primer intento de modificar readme.

Se le cambio el nombre

Agrego palabras

git add .

git commit -am "le pongo mensaje"

git push

Con esos tres modifico en github los archivos que voy cambiando en Sublime Text